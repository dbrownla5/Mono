# commerce-workflow

**Intent:** the connected sell/operations flow — the **dashboard**, the
**reseller** tool, and the **handshake** piece were all meant to work together as
one workflow. They live here together so that connection is visible.

**Provisional source repos:** `Dashboard-`, `handshake-dashboard`,
`Resell-Google-App-v5.3.2026`, `Propertyv050826` (?), `SHIPWEIGHT` (?)

**Flag:** watch for Firebase / cloud lock-in here especially — commerce apps
tend to bury it. Flag for removal, don't build on it.

As this bucket is worked, extracted pieces land in:
- `tools/` — the working commerce pieces
- `content/` — any listings, copy, or business logic worth keeping as text

_Status: not started._
