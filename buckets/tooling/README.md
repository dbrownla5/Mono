# tooling

**Intent:** the reusable machinery — the agents, content-extraction engines, and
Claude Code config/skills that help run everything else. Merges the former
`business-agent` and `dev-tooling` groups.

**Provisional source repos:** `BUSINESS-AGENT`, `Content-Extractor`,
`repo-digest`, `claude-code-best-practice`, `skills`

**Why it's one group:** these are tools, not products. If any of them work, they
speed up processing the rest — so this group is worth landing early.

_Status: not started._
