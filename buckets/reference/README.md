# reference

**Intent:** the retired repos, forks, boilerplates, and accidental repos. These
are **salvage only** — we don't trust them as active, and most may have nothing
worth keeping. Triage quickly, take anything real, skip the rest.

**Provisional source repos:** `WEBREBUILD` (?), `May24` (?),
`team-project-build` (?), `ReplitFinal_referenceonlyretired`, `for_referencev5.1`,
`replit_updatedv5.5.2026`, `solid-couscous` (?), `nextjs-boilerplate`,
`vercel-ai-gateway-demo`, `vero-for-vercel`, `git-clone-...-vero`, `ai-pricing`,
`antigravity_phone_chat`, `github-action`, `whisper`, `dayna-brown` (?)

**Careful:** the `?` repos (`WEBREBUILD`, `May24`, `team-project-build`,
`solid-couscous`, `dayna-brown`) have unknown contents — they might actually hold
real WLC or commerce work under a throwaway name. Triage before trusting the
"reference" label.

Anything real found here gets **moved out** to its proper intent bucket. What
stays here is genuinely reference-only.

_Status: not started._
