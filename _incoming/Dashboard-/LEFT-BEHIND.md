# LEFT BEHIND — Dashboard-

Not staged (still in the source repo on GitHub, untouched):

| Path | Why |
|---|---|
| `src/`, `server.ts`, `vite.config.ts`, `index.html` | The Gemini dashboard app code — a later *tools* pass, not this content-first one. Depends on `GEMINI_API_KEY` (Google lock-in). |
| `package.json`, `package-lock.json`, `tsconfig.json`, `.env.example` | Build/config plumbing. |
| `metadata.json` | AI Studio app metadata. |

## Redaction note

`outputs/session_summary.md` was staged with **personal phone number and personal
email redacted**. The unredacted original remains in the source repo (which is
already public). If you want the contact info kept somewhere, it belongs in the
**private** home base, not here.

Nothing decided; any left-behind code can be pulled in on request.
